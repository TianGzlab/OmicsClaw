# OmicsClaw — Core dependencies (Tier 1)
# Synced with pyproject.toml [project.dependencies]
#
# For the full optional methods (Tier 3):
#   pip install -e ".[full]"
# For deep learning spatial domains (Tier 2):
#   pip install -e ".[spatial-domains]"
# For development tools (Tier 4):
#   pip install -e ".[dev]"

# Single-cell / spatial core
scanpy>=1.9.0,<2.0
anndata>=0.11.0
squidpy>=1.2.0

# Data science
numpy>=1.24
pandas>=2.0
scipy>=1.7.0
scikit-learn>=1.3

# Clustering & graph
igraph>=0.9.0
leidenalg>=0.8.0

# Dimensionality reduction
umap-learn>=0.5.0

# Visualization
matplotlib>=3.7
seaborn>=0.11.0
Pillow>=8.0.0

# Data validation
pydantic>=2.0.0,<3.0

# HVG seurat_v3 flavor
scikit-misc>=0.5.0

# Single-cell specific
scrublet>=0.2.3

# pkg_resources fallback for older packages
setuptools>=42
