---
phase: 19-scop-renderer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/singlecell/_lib/viz/r/density.R
  - skills/singlecell/_lib/viz/r/sankey.R
  - skills/singlecell/_lib/viz/r/stat.R
  - skills/singlecell/_lib/viz/r/registry.R
autonomous: true
requirements: [SCOP-01, SCOP-02, SCOP-04]
must_haves:
  truths:
    - "plot_cell_density produces a ridgeline PNG showing cell density grouped by cell type or along pseudotime"
    - "plot_proportion_test produces a pointrange PNG with FDR annotation and log2FD threshold lines"
    - "plot_cell_sankey produces an alluvial diagram showing cell type transitions between two categorical columns"
    - "ggalluvial R package is installed and available for sankey rendering"
  artifacts:
    - path: "skills/singlecell/_lib/viz/r/density.R"
      provides: "plot_cell_density renderer using ggridges"
      contains: "plot_cell_density"
    - path: "skills/singlecell/_lib/viz/r/stat.R"
      provides: "plot_proportion_test renderer added to existing stat.R"
      contains: "plot_proportion_test"
    - path: "skills/singlecell/_lib/viz/r/sankey.R"
      provides: "plot_cell_sankey renderer using ggalluvial"
      contains: "plot_cell_sankey"
    - path: "skills/singlecell/_lib/viz/r/registry.R"
      provides: "Registry updated with 3 new renderers from this plan"
      contains: "plot_cell_density"
  key_links:
    - from: "skills/singlecell/_lib/viz/r/registry.R"
      to: "skills/singlecell/_lib/viz/r/density.R"
      via: "source() + R_PLOT_REGISTRY entry"
      pattern: "source.*density\\.R"
    - from: "skills/singlecell/_lib/viz/r/registry.R"
      to: "skills/singlecell/_lib/viz/r/sankey.R"
      via: "source() + R_PLOT_REGISTRY entry"
      pattern: "source.*sankey\\.R"
---

<objective>
Create 3 new R Enhanced renderers: cell density ridgeline plot, proportion test pointrange plot, and cell type Sankey diagram. Install ggalluvial dependency and register all renderers.

Purpose: These 3 renderers cover the most-requested missing visualizations from scop: density distribution (CellDensityPlot), proportion testing results (ProportionTestPlot), and cell type flow mapping (CellStatPlot sankey).

Output: density.R (new), sankey.R (new), stat.R (extended), registry.R (updated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-scop-renderer/19-RESEARCH.md
@skills/singlecell/_lib/viz/r/common.R
@skills/singlecell/_lib/viz/r/registry.R
@skills/singlecell/_lib/viz/r/stat.R

<interfaces>
<!-- Existing shared utilities from common.R that all renderers must use -->

```r
theme_omics(base_size = 11)           # Publication theme
omics_palette(n, type = "categorical") # Color palette helper
ggsave_standard(plot, path, width, height, dpi) # PNG save with dir creation
parse_kv(args_vec)                     # CLI key=value parser
`%||%`                                 # Null-coalescing operator
```

<!-- Registry pattern: every renderer registers in R_PLOT_REGISTRY named list -->
```r
R_PLOT_REGISTRY <- list(
  plot_xxx = plot_xxx,
  ...
)
```

<!-- Renderer signature (mandatory) -->
```r
plot_xxx <- function(data_dir, out_path, params) {
  tryCatch({
    # 1. Read CSV(s) from data_dir
    # 2. Build ggplot
    # 3. ggsave_standard(p, out_path, width, height)
  }, error = function(e) {
    cat("ERROR:", conditionMessage(e), "\n", file = stderr())
    quit(status = 1)
  })
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install ggalluvial + create density.R and sankey.R</name>
  <files>skills/singlecell/_lib/viz/r/density.R, skills/singlecell/_lib/viz/r/sankey.R</files>
  <action>
**Step A: Install ggalluvial**

Run in R with OMICSCLAW_R_LIBS:
```r
install.packages("ggalluvial", lib = Sys.getenv("OMICSCLAW_R_LIBS"), repos = "https://cloud.r-project.org")
```
Verify: `requireNamespace("ggalluvial", quietly = TRUE)` returns TRUE.

**Step B: Create density.R** (new file)

Create `skills/singlecell/_lib/viz/r/density.R` with one renderer: `plot_cell_density`.

Implementation per RESEARCH Pattern 1:
- Read CSV: try `pseudotime_points.csv` first (has pseudotime column), fall back to `annotation_embedding_points.csv`
- Detect numeric feature column: try params[["feature"]] first, then auto-detect from c("pseudotime", "cytotrace_score", "dpt_pseudotime", "velocity_pseudotime")
- Detect group column: try params[["group_by"]] first, then auto-detect from c("cell_type", "group", "cluster", "leiden", "louvain")
- Use `ggridges::geom_density_ridges(scale = 1.2, alpha = 0.7)` for ridgeline plot
- Color fill by group using `omics_palette()`
- If no group column found, use single density plot with `geom_density()`
- Dynamic height: `max(4, n_groups * 0.6 + 1.5)` inches
- Include the standard tryCatch error handler wrapper
- Use `theme_omics()` and `ggsave_standard()`

**Step C: Create sankey.R** (new file)

Create `skills/singlecell/_lib/viz/r/sankey.R` with one renderer: `plot_cell_sankey`.

Implementation per RESEARCH Pattern 4:
- `library(ggalluvial)` at top (with requireNamespace guard -- if missing, fall back to stacked bar)
- Read CSV: try `annotation_embedding_points.csv` from data_dir
- Get column names from params: `left_col` (default "cluster"), `right_col` (default "cell_type")
- Build frequency table: `as.data.frame(table(df[[left_col]], df[[right_col]]))` then filter Freq > 0
- Use `ggalluvial::geom_alluvium(aes(fill = Right), width = 1/6, alpha = 0.7)` + `ggalluvial::geom_stratum(width = 1/6, fill = "grey90", color = "black")`
- Add `geom_text(stat = "stratum", aes(label = after_stat(stratum)), size = 3)`
- Color fill by Right column using `omics_palette()`
- Use `scale_x_discrete(limits = c(left_col, right_col), expand = c(0.1, 0.1))`
- `theme_omics()` + `ggsave_standard()`
- **Fallback if ggalluvial not installed**: create a stacked bar chart with geom_col instead

Use the exact code examples from RESEARCH.md as starting templates, adapting for edge cases (empty data, missing columns).
  </action>
  <verify>
    <automated>cd /data2/liying/git/OmicsClaw && Rscript -e 'cat("ggalluvial:", requireNamespace("ggalluvial", quietly=TRUE), "\n")' && test -f skills/singlecell/_lib/viz/r/density.R && test -f skills/singlecell/_lib/viz/r/sankey.R && echo "OK"</automated>
  </verify>
  <done>density.R contains plot_cell_density function with ggridges ridgeline pattern. sankey.R contains plot_cell_sankey function with ggalluvial alluvial pattern and fallback. ggalluvial package is installed.</done>
</task>

<task type="auto">
  <name>Task 2: Extend stat.R with plot_proportion_test + register all 3 in registry.R</name>
  <files>skills/singlecell/_lib/viz/r/stat.R, skills/singlecell/_lib/viz/r/registry.R</files>
  <action>
**Step A: Add plot_proportion_test to stat.R**

Append a new function `plot_proportion_test` to the end of `skills/singlecell/_lib/viz/r/stat.R` (after plot_cell_proportion, before the file ends).

Implementation per RESEARCH Pattern 2:
- Read `proportion_test_results.csv` from data_dir
- Expected columns: `clusters`, `obs_log2FD`, `boot_CI_2.5`, `boot_CI_97.5`, `FDR`
- Params: `FDR_threshold` (default 0.05), `fold_threshold` (default 1.5 -- compute log2FD_threshold = log2(fold_threshold))
- Compute significance: `FDR < FDR_threshold & abs(obs_log2FD) > log2FD_threshold` -> "Significant" else "n.s."
- Order clusters by obs_log2FD descending
- Plot: `geom_pointrange(aes(x=clusters, y=obs_log2FD, ymin=boot_CI_2.5, ymax=boot_CI_97.5, color=significance))`
- Add `geom_hline` at +/- log2FD_threshold (dashed grey) and at 0 (solid black)
- `scale_color_manual(values = c("Significant" = "#E41A1C", "n.s." = "grey60"))`
- `coord_flip()` for horizontal orientation
- Labels: x = "Cell Type", y = "log2(Fold Difference)", title = "Proportion Test"
- `theme_omics()` + `ggsave_standard()`
- Handle edge case: if `comparison` column exists, include it in subtitle
- Dynamic width: `max(6, n_clusters * 0.5 + 2)` where n_clusters = length(unique(clusters))
- Standard tryCatch wrapper

**Step B: Update registry.R**

1. Add two new `source()` lines after the existing `source(file.path(script_dir, "stat.R"))`:
   ```r
   source(file.path(script_dir, "density.R"))
   source(file.path(script_dir, "sankey.R"))
   ```

2. Add 3 new entries to `R_PLOT_REGISTRY`:
   ```r
   plot_cell_density      = plot_cell_density,
   plot_proportion_test   = plot_proportion_test,
   plot_cell_sankey       = plot_cell_sankey,
   ```

3. Do NOT add the Plan 02 renderers yet -- Plan 02 will add those in a separate registry.R update (Wave 1 parallel execution means Plan 02 modifies the same file, so coordinate by having Plan 02 also add its entries).

**IMPORTANT**: Since Plan 02 also modifies registry.R in Wave 1, both plans must be aware of the other's additions. The executor should add ALL new entries (from both plans) in one pass if executing sequentially, or use the wave system's file conflict detection. To be safe: this task adds the `source()` lines and registry entries for density.R, sankey.R, and plot_proportion_test only. Plan 02 will add its own source() + registry entries.

**CORRECTION**: registry.R is modified by BOTH Plan 01 and Plan 02. To avoid conflicts, restructure: Plan 01 creates density.R + sankey.R + extends stat.R only. registry.R updates will ALL happen in Plan 03 (Wave 2) after both R file plans complete. This avoids merge conflicts.

Actually, re-reading the constraint: Wave 1 parallel plans must not share files. So registry.R must be in exactly ONE plan or in Wave 2. Move all registry.R updates to Plan 03.

Revised Step B: Do NOT modify registry.R in this task. Only create/extend the R renderer files. Plan 03 (Wave 2) will handle all registry.R updates and Python wiring.
  </action>
  <verify>
    <automated>cd /data2/liying/git/OmicsClaw && grep -c "plot_proportion_test" skills/singlecell/_lib/viz/r/stat.R && echo "proportion_test added to stat.R"</automated>
  </verify>
  <done>stat.R contains plot_proportion_test function with geom_pointrange pattern, FDR annotation, and log2FD threshold lines. density.R and sankey.R exist from Task 1. All 3 new renderer functions are complete and ready for registry integration in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file input | R reads CSV files from figure_data/ directory -- untrusted column names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | T (Tampering) | CSV column names | accept | Column names come from Python skill code, not user input; low risk |
| T-19-02 | D (DoS) | Large CSV files | accept | ggridges/ggalluvial handle large data via ggplot2 lazy evaluation; existing pattern |
</threat_model>

<verification>
All 3 R renderer files exist and contain properly structured functions with tryCatch error handling:
- `skills/singlecell/_lib/viz/r/density.R` -- plot_cell_density
- `skills/singlecell/_lib/viz/r/stat.R` -- plot_proportion_test (appended)
- `skills/singlecell/_lib/viz/r/sankey.R` -- plot_cell_sankey
- ggalluvial package installed
</verification>

<success_criteria>
1. density.R creates ridgeline density plot from embedding/pseudotime CSV with ggridges
2. stat.R contains plot_proportion_test with pointrange + FDR coloring + threshold lines
3. sankey.R creates alluvial diagram from two categorical columns with ggalluvial (or stacked bar fallback)
4. ggalluvial is installed and importable
5. All functions follow the `function(data_dir, out_path, params)` signature with tryCatch
</success_criteria>

<output>
After completion, create `.planning/phases/19-scop-renderer/19-01-SUMMARY.md`
</output>
