# R Enhanced Visualisation

OPTIONAL: delete this file if the skill has no R-side replot path.

If the skill ships an `r_visualization/` directory with ggplot2 templates,
document them here.  Each template gets one section.

## `<template_name>.R`

- **Renderer:** `plot_<thing>`
- **Inputs:** which figure_data rows it consumes.
- **Tunables:** `--top-n`, `--font-size`, `--width`, `--height`, …
