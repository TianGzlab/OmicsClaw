# Single-Cell Omics — Skill Index

> Auto-generated by `scripts/generate_domain_index.py`.
> Do not edit by hand — changes will be overwritten.

**Domain key:** `singlecell`  
**Skill count:** 30  
**Primary data types:** h5ad, h5, loom, mtx  

scRNA-seq + scATAC-seq: FASTQ→counts, QC, filter, doublet removal, normalize→HVG→PCA→UMAP→cluster, annotation, DE, trajectory, velocity, GRN, CCC.

## Skills

- `sc-ambient-removal` — Remove ambient RNA contamination from droplet-based single-cell RNA-seq using a simple subtraction path, CellBender, or SoupX. The wrapper exposes only the parameters that are actually wired into the current implementation.
  triggers: ambient RNA, ambient removal, cellbender, contamination, background RNA
- `sc-batch-integration` — Integrate multi-sample scRNA-seq data with Harmony, scVI, scANVI, BBKNN, Scanorama, or supported R-backed integration methods.
  triggers: batch integration, batch effect, harmony, scvi, bbknn, merge samples
- `sc-cell-annotation` — Annotate cell types from normalized scRNA-seq data using marker scoring, CellTypist, PopV-style reference mapping, lightweight KNNPredict-style mapping, SingleR, or scmap through shared Python/R backends.
- `sc-cell-communication` — Cell-cell communication analysis for annotated scRNA-seq data using a built-in ligand-receptor scorer, LIANA, CellPhoneDB, CellChat, or a NicheNet R path.
  triggers: cell communication, cell-cell communication, ligand receptor, cellchat, liana, cellphonedb, nichenet
- `sc-clustering` — Build the neighbor graph, run a low-dimensional embedding, and cluster single-cell data from a normalized scRNA AnnData object.
- `sc-count` — Default scRNA counting route. Turn FASTQ or existing Cell Ranger, STARsolo, SimpleAF / Alevin-fry, or kb-python outputs into a downstream-ready standardized AnnData.
  triggers: Cell Ranger count, STARsolo count, fastq to adata, raw single-cell counting, generate count matrix
- `sc-cytotrace` — Predict cell differentiation potency from scRNA-seq data using gene expression complexity as a proxy for stemness.
- `sc-de` — Differential expression for single-cell RNA-seq using exploratory Scanpy ranking, R-backed MAST, or replicate-aware pseudobulk DESeq2. The wrapper separates cluster/group marker ranking from sample-aware condition DE.
  triggers: differential expression, marker genes, de analysis, wilcoxon, pseudo-bulk
- `sc-differential-abundance` — Sample-aware differential abundance and compositional analysis for scRNA-seq using Milo, scCODA, or an exploratory proportion-based fallback.
- `sc-doublet-detection` — Annotate putative doublets in single-cell RNA-seq data using Scrublet, DoubletDetection, DoubletFinder, scDblFinder, or scds. The wrapper preserves the current AnnData matrix semantics, standardizes output columns in `obs`, and exports a reusable figure/table gallery.
- `sc-drug-response` — Drug response prediction from scRNA-seq (CaDRReS-Sc, simple correlation)
- `sc-enrichment` — Statistical enrichment analysis for single-cell RNA-seq using ORA or preranked GSEA on marker or differential-expression rankings. This skill is for GO/KEGG/Reactome/Hallmark term significance, not per-cell pathway activity scoring.
  triggers: sc enrichment, single-cell enrichment, GO enrichment, KEGG enrichment, GSEA, ORA, pathway enrichment
- `sc-fastq-qc` — Start here if you have raw single-cell FASTQ files. Checks read quality before counting with FastQC and MultiQC when available, plus a stable local fallback summary.
  triggers: scRNA FASTQ QC, FastQC single-cell, MultiQC single-cell, raw read quality, read-level QC
- `sc-filter` — Filter cells and genes from single-cell RNA-seq AnnData objects using QC-derived thresholds or tissue presets. This wrapper removes low-quality cells/genes but does not normalize, cluster, or annotate the dataset.
  triggers: filter cells, cell filtering, gene filtering, remove low quality, qc filtering, tissue-specific thresholds
- `sc-gene-programs` — Discover de novo gene programs and per-cell usage scores from scRNA-seq data using cNMF-compatible or NMF workflows.
- `sc-grn` — Infer gene regulatory networks from scRNA-seq using the pySCENIC workflow: GRNBoost2 for adjacency inference, cisTarget-style motif pruning, and AUCell regulon scoring.
  triggers: grn, gene regulatory, scenic, pyscenic, regulon, transcription factor, grnboost
- `sc-in-silico-perturbation` — In-silico perturbation analysis for scRNA-seq. Simulates the effect of knocking out a target gene and identifies differentially regulated genes.
- `sc-markers` — Rank cluster marker genes from normalized single-cell AnnData using Scanpy-backed Wilcoxon, t-test, or logistic-regression methods. The wrapper standardizes outputs for downstream annotation and review.
- `sc-metacell` — Compress scRNA-seq data into metacell-level summaries using SEACells or a lightweight k-means aggregation fallback.
- `sc-multi-count` — Merge multiple single-sample scRNA-seq count matrices (from sc-count) into one downstream-ready AnnData with sample labels.
  triggers: merge count matrices, multi-sample count, aggregate samples, combine count outputs, cellranger aggr alternative
- `sc-pathway-scoring` — Single-cell pathway and gene-set activity scoring for preprocessed scRNA-seq data using AUCell or a lightweight normalized-expression module-score path.
  triggers: pathway score, pathway scoring, gene set score, module score, pathway activity, signature score
- `sc-perturb` — Single-cell perturbation analysis for scRNA-seq perturbation screens using the official pertpy Mixscape workflow.
- `sc-perturb-prep` — Prepare perturbation-ready scRNA AnnData objects by merging barcode-to-guide assignments into expression data and exporting a downstream-safe h5ad for `sc-perturb`.
- `sc-preprocessing` — Base scRNA preprocessing after QC: QC-aware filtering, normalization, highly variable gene selection, and PCA.
  triggers: single cell preprocess, scRNA preprocessing, normalize hvg pca, base preprocessing, Seurat preprocessing, SCTransform preprocessing
- `sc-pseudotime` — Single-cell pseudotime and lineage inference after clustering, with DPT, Palantir, VIA, CellRank, or Slingshot plus post-hoc trajectory gene ranking.
  triggers: pseudotime, trajectory, lineage, diffusion pseudotime, palantir, via, cellrank, monocle3
- `sc-qc` — Review cell quality before filtering. Computes counts, detected genes, mitochondrial percentage, and ribosomal percentage, but does not remove cells.
  triggers: scRNA QC, single-cell QC, quality control, mitochondrial percentage, ribosomal percentage, QC violin, QC scatter, n genes per cell
- `sc-standardize-input` — Start here if you already have an external single-cell h5ad. Fixes the AnnData contract so downstream OmicsClaw scRNA skills can use it safely.
  triggers: standardize AnnData, fix scRNA input, canonicalize single-cell input, prepare AnnData, input contract
- `sc-velocity` — Run scVelo on a velocity-ready h5ad using stochastic, dynamical, or steady-state modes. Use `sc-velocity-prep` first if spliced/unspliced layers are missing.
  triggers: rna velocity, velocity, scvelo, spliced unspliced, cellular dynamics, velovi, velocity pseudotime
- `sc-velocity-prep` — Start here for RNA velocity when you have Cell Ranger BAM, loom, or STARsolo Velocyto output. Creates the spliced and unspliced layers needed by scVelo.
  triggers: RNA velocity prep, prepare spliced unspliced layers, velocyto, starsolo velocyto, velocity-ready AnnData
- `scatac-preprocessing` — Single-cell ATAC-seq preprocessing with a Signac-style TF-IDF + LSI workflow. Performs cell and peak filtering, top-peak selection, TF-IDF normalization, latent semantic indexing, neighborhood graph construction, UMAP, and Leiden clustering, then exports a downstream-ready AnnData plus a standard OmicsClaw gallery and reproducibility bundle.
  triggers: scATAC preprocessing, single-cell ATAC preprocessing, ATAC TF-IDF LSI, chromatin accessibility clustering, scATAC UMAP Leiden
